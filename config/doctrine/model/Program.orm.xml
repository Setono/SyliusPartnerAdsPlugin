<?xml version="1.0" encoding="UTF-8"?>

<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <mapped-superclass name="Setono\SyliusPartnerAdsPlugin\Model\Program" table="setono_sylius_partner_ads__program">

        <id name="id" type="integer">
            <generator strategy="AUTO"/>
        </id>

        <field name="programId" type="integer" unique="true" />
        <field name="enabled" column="enabled" type="boolean" />

        <!-- one program per channel; see also the UniqueEntity constraint in config/validation/Program.xml -->
        <one-to-one field="channel" target-entity="Sylius\Component\Channel\Model\ChannelInterface">
            <join-column name="channel_id" nullable="false" unique="true" />
        </one-to-one>
    </mapped-superclass>

</doctrine-mapping>
